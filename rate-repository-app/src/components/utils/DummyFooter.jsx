import { View, StyleSheet } from "react-native"

const styles = StyleSheet.create({
  footer: {
    height: 500,
  },
})

const DummyFooter = () => <View style={styles.footer} />

export default DummyFooter
